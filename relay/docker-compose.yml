services:
  anonshare-relay:
    build: .
    container_name: anonshare-relay
    restart: always
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - HOST=0.0.0.0
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-anonshare-secret-password-change-me}
      - NODE_ENV=production
