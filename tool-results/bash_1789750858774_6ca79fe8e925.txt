===== app.css =====
/* ------------------------------------------------------------------
   anonshare - vanta black, hairline borders, monospace chrome.
   Sharp corners everywhere. The only round thing is a person.
------------------------------------------------------------------ */

:root{
  --bg:#000; --raise:#080808; --panel:#0b0b0b; --line:#1c1c1c; --line2:#2a2a2a;
  --fg:#e7e7e7; --mut:#6d6d6d; --dim:#3d3d3d;
  --accent:#4c8dff; --ok:#3ddc84; --warn:#e8b339; --danger:#ff5c4d;
  --mono:ui-monospace,SFMono-Regular,"SF Mono",Menlo,"Cascadia Mono",Consolas,monospace;
  --sans:ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
  --bar:38px; --edfont:13px;
  --ease:cubic-bezier(.22,.61,.36,1);
}
[data-theme=light]{
  --bg:#fff; --raise:#fafafa; --panel:#f7f7f7; --line:#e5e5e5; --line2:#d8d8d8;
  --fg:#111; --mut:#767676; --dim:#c2c2c2; --accent:#0b62e0;
}
[data-theme=dracula]{
  --bg:#282a36; --raise:#21222c; --panel:#1e1f29; --line:#44475a; --line2:#6272a4;
  --fg:#f8f8f2; --mut:#6272a4; --dim:#44475a; --accent:#bd93f9; --ok:#50fa7b; --warn:#ffb86c; --danger:#ff5555;
}
[data-theme=nord]{
  --bg:#2e3440; --raise:#3b4252; --panel:#434c5e; --line:#4c566a; --line2:#4c566a;
  --fg:#eceff4; --mut:#d8dee9; --dim:#4c566a; --accent:#88c0d0; --ok:#a3be8c; --warn:#ebcb8b; --danger:#bf616a;
}
[data-theme=monokai]{
  --bg:#272822; --raise:#1e1f1c; --panel:#171814; --line:#3e3d32; --line2:#49483e;
  --fg:#f8f8f2; --mut:#75715e; --dim:#49483e; --accent:#66d9ef; --ok:#a6e22e; --warn:#fd971f; --danger:#f92672;
}

*{box-sizing:border-box}
idden]{display:none!important}
html,body{height:100%;margin:0}
body{
  background:var(--bg);color:var(--fg);
  font:13px/1.55 var(--sans);
  -webkit-font-smoothing:antialiased;overflow:hidden;
  -webkit-tap-highlight-color:transparent;
  transition:background-color .2s var(--ease),color .2s var(--ease);
}
button,input,select,textarea{font:inherit;color:inherit}
button{cursor:pointer;background:none;border:0;padding:0}
a{color:var(--accent)}
.grow{flex:1}
.mono{font-family:var(--mono)}

@media(prefers-reduced-motion:reduce){
  *,*::before,*::after{animation-duration:.001ms!important;animation-iteration-count:1!important;transition-duration:.001ms!important;scroll-behavior:auto!important}
}

/* --------------------------------------------------------- controls */
.btn{
  display:inline-flex;align-items:center;justify-content:center;gap:6px;
  height:28px;padding:0 10px;flex:none;white-space:nowrap;
  border:1px solid var(--line2);background:var(--raise);color:var(--fg);
  font:500 12px/1 var(--mono);letter-spacing:.02em;
  transition:background .16s var(--ease),border-color .16s var(--ease),transform .16s var(--ease),box-shadow .16s var(--ease);
}
.btn:hover{background:var(--panel);border-color:var(--mut)}
.btn:active{transform:translateY(1px) scale(0.97)}
.btn:disabled{opacity:.4;cursor:default}
.btn.primary{background:var(--accent);border-color:var(--accent);color:#fff}
.btn.primary:hover{filter:brightness(1.12)}
.btn.danger{border-color:var(--danger);color:var(--danger);background:transparent}
.btn.danger:hover{background:var(--danger);color:#fff}
.btn.ico{width:28px;padding:0;position:relative;font-size:14px}
.btn.ico svg.ic{display:block;pointer-events:none}
.btn.flat{border-color:transparent;background:transparent}
.btn.flat:hover{border-color:var(--line2)}
.btn.block{width:100%;height:40px;font-size:13px}
.btn.sel{font-family:var(--mono);max-width:112px;padding:0 4px;-webkit-appearance:none;appearance:none;text-align:center}
.btn.go{width:40px;height:40px;font-size:16px;border-left:0}

/* Distinct visual active / pressed states for all toggle buttons */
.btn.active, .btn[aria-pressed="true"]{
  background: var(--accent)!important;
  border-color: var(--accent)!important;
  color: #fff!important;
  box-shadow: 0 0 10px rgba(76,141,255,.55);
}
.btn.active svg, .btn[aria-pressed="true"] svg{
  stroke: #fff!important;
  color: #fff!important;
}
.btn.primary.active svg{
  fill: #fff!important;
}

#voiceBtn.speaking{
  box-shadow: 0 0 12px #3ddc84, inset 0 0 4px #3ddc84!important;
  border-color: #3ddc84!important;
  animation: pulse-ring 1.2s infinite;
}
#voiceBtn.muted{
  background: rgba(245,158,11,.2)!important;
  border-color: #f59e0b!important;
  color: #f59e0b!important;
  box-shadow: 0 0 10px rgba(245,158,11,.45)!important;
}
#voiceBtn.muted svg{
  stroke: #f59e0b!important;
  color: #f59e0b!important;
}
.av.speaking{
  box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px #3ddc84!important;
  animation: pulse-ring 1.2s infinite;
}
@keyframes pulse-ring{0%{box-shadow:0 0 0 0 rgba(61,220,132,.7)} 70%{box-shadow:0 0 0 8px rgba(61,220,132,0)} 100%{box-shadow:0 0 0 0 rgba(61,220,132,0)}}

.hub-tab-btn{
  margin-left:auto;
  height:24px;
  padding:0 9px;
  font-size:11px;
  background:var(--raise);
  border:1px solid var(--line2);
  gap:6px;
  border-radius:3px;
}
.hub-tab-btn:hover{background:var(--panel);border-color:var(--accent)}
.hub-tab-btn.active{background:var(--accent)!important;color:#fff!important;border-color:var(--accent)!important}

.nd{
  position:absolute;top:-4px;right:-4px;min-width:14px;height:14px;padding:0 3px;
  border-radius:7px;background:var(--accent);color:#fff;
  font:600 9px/14px var(--mono);text-align:center;box-shadow:0 0 0 2px var(--raise);
}
:focus-visible{outline:2px solid var(--accent);outline-offset:1px}

.in{
  width:100%;height:36px;padding:0 10px;
  border:1px solid var(--line2);background:var(--bg);color:var(--fg);
  font:13px/1 var(--sans);
}
.in:focus{outline:0;border-color:var(--accent);box-shadow:inset 0 0 0 1px var(--accent)}
.in::placeholder{color:var(--dim)}
select.in{-webkit-appearance:none;appearance:none;font-family:var(--mono);font-size:12px}
.mono-in{font-family:var(--mono);font-size:12px}
.lbl{
  display:block;margin:16px 0 6px;
  font:500 10px/1 var(--mono);letter-spacing:.14em;text-transform:uppercase;color:var(--mut);
}
.lnk{color:var(--accent);font:11px/1 var(--mono);text-decoration:underline;text-underline-offset:2px}
.errline{margin:12px 0 0;color:var(--danger);font:12px/1.45 var(--mono)}
.fineprint{margin:10px 0 0;color:var(--mut);font-size:11px;line-height:1.6}
.sec{
  margin:22px 0 8px;font:500 10px/1 var(--mono);letter-spacing:.14em;
  text-transform:uppercase;color:var(--mut);
}
.sec.danger{color:var(--danger)}

/* password field with a reveal toggle */
.passrow{display:flex;align-items:stretch}
.passrow .in{flex:1;min-width:0}
.eye{
  flex:none;padding:0 10px;border:1px solid var(--line2);border-left:0;
  background:var(--raise);color:var(--mut);
  font:10px/1 var(--mono);letter-spacing:.1em;text-transform:uppercase;
}
.eye:hover{color:var(--fg)}
.eye[aria-pressed=true]{color:var(--accent)}

/* skip link, for keyboard/screen-reader users to bypass the hero and
   go straight to the editor once a room is open */
.skip-link{
  position:absolute;left:-9999px;top:0;z-index:100;
  background:var(--accent);color:#fff;padding:8px 14px;
  font:600 12px/1 var(--mono);text-decoration:none;
}
.skip-link:focus{left:8px;top:8px}

/* ============================================================== gate */
#gate{
  position:fixed;inset:0;z-index:40;overflow:auto;background:var(--bg);
  display:flex;flex-direction:column;
  scroll-behavior:smooth;-webkit-overflow-scrolling:touch;overscroll-behavior-y:contain;
}
.hero{
  flex:1;display:grid;gap:52px;align-items:center;
  grid-template-columns:minmax(300px,360px) minmax(0,1fr);
  max-width:1120px;width:100%;margin:0 auto;padding:56px 28px 40px;
}
.gwrap{width:100%;max-width:360px}
.logo{font:600 15px/1 var(--mono);letter-spacing:.02em;display:flex;align-items:center;gap:7px}
.logo .caret{color:var(--accent);animation:blink 1.2s steps(2) infinite}
@keyframes blink{50%{opacity:.15}}
.htitle{
  margin:22px 0 0;font:600 27px/1.22 var(--sans);letter-spacing:-.022em;
  max-width:15ch;
}
.tag{color:var(--mut);margin:12px 0 28px;font-size:12.5px;line-height:1.65}
.rule{display:flex;align-items:center;gap:12px;margin:20px 0;color:var(--dim);font:10px/1 var(--mono);letter-spacing:.14em;text-transform:uppercase}
.rule::before,.rule::after{content:"";flex:1;height:1px;background:var(--line)}
.codebox{display:flex}
.code-in{
  flex:1;height:40px;min-width:0;padding:0 14px;
  border:1px solid var(--line2);background:var(--bg);color:var(--fg);
  font:600 17px/1 var(--mono);letter-spacing:.42em;text-transform:uppercase;
}
.code-in:focus{outline:0;border-color:var(--accent);box-shadow:inset 0 0 0 1px var(--accent)}
.code-in::placeholder{color:var(--dim);letter-spacing:.42em}

/* ---- the self-running demo, so the page shows instead of tells ---- */
.demoWrap{
  border:1px solid var(--line2);background:var(--raise);
  display:flex;flex-direction:column;min-width:0;height:330px;
  box-shadow:0 30px 70px rgba(0,0,0,.55);
}
[data-theme=light] .demoWrap{box-shadow:0 24px 50px rgba(0,0,0,.10)}
.demoBar{display:flex;align-items:center;gap:0;height:32px;padding:0 10px;border-bottom:1px solid var(--line)}
.dcode{font:600 11px/1 var(--mono);letter-spacing:.22em;color:var(--mut)}
.dav{
  width:20px;height:20px;border-radius:50%;display:grid;place-items:center;
  font:600 8px/1 var(--mono);color:#000;margin-left:-6px;box-shadow:0 0 0 2px var(--raise);
}
#demo{flex:1;min-height:0;overflow:hidden;position:relative}
#demo .cm-editor{background:transparent!important}
#demo .cm-scroller{overflow:hidden}
.demoFoot{
  height:24px;display:flex;align-items:center;gap:7px;padding:0 10px;
  border-top:1px solid var(--line);
  font:10px/1 var(--mono);letter-spacing:.08em;text-transform:uppercase;color:var(--mut);
}
.ddot{width:6px;height:6px;border-radius:50%;background:var(--ok);animation:beat 1.4s infinite}

/* ---- feature strips ---- */
.strips{
  display:grid;gap:1px;background:var(--line);
  grid-template-columns:repeat(3,1fr);
  border-top:1px solid var(--line);border-bottom:1px solid var(--line);
}
.strip{background:var(--bg);padding:24px 28px;transition:transform .18s var(--ease)}
.strip:hover{transform:translateY(-2px)}
.strip .si{font-size:15px;line-height:1}
.strip b{display:block;margin:12px 0 6px;font:600 12px/1 var(--mono);letter-spacing:.02em}
.strip p{margin:0;color:var(--mut);font-size:12px;line-height:1.65;max-width:34ch}

/* ---- how it works + FAQ ---- */
.sectitle{margin:0 0 30px;font:600 20px/1.25 var(--sans);letter-spacing:-.02em}
.howBlock{max-width:1120px;width:100%;margin:0 auto;padding:52px 28px;border-bottom:1px solid var(--line)}
.steps{display:grid;gap:1px;background:var(--line);grid-template-columns:repeat(3,1fr);border:1px solid var(--line)}
.step{background:var(--bg);padding:24px 26px;transition:transform .18s var(--ease)}
.step:hover{transform:translateY(-2px)}
.stepno{
  display:inline-flex;align-items:center;justify-content:center;width:26px;height:26px;
  border:1px solid var(--line2);font:600 12px/1 var(--mono);color:var(--accent);margin-bottom:14px;
}
.step b{display:block;margin:0 0 6px;font:600 13px/1.35 var(--sans)}
.step p{margin:0;color:var(--mut);font-size:12.5px;line-height:1.65;max-width:32ch}

.faqBlock{max-width:1120px;width:100%;margin:0 auto;padding:52px 28px;border-bottom:1px solid var(--line)}
.faqlist{border-top:1px solid var(--line)}
.faq{border-bottom:1px solid var(--line)}
.faq summary{
  list-style:none;cursor:pointer;padding:16px 2px;
  font:600 13px/1.4 var(--sans);color:var(--fg);
  display:flex;align-items:center;justify-content:space-between;gap:14px;
  transition:color .15s var(--ease);
}
.faq summary:hover{color:var(--accent)}
.faq summary::-webkit-details-marker{display:none}
.faq summary::after{content:"+";flex:none;color:var(--mut);font:400 17px/1 var(--mono);transition:color .15s var(--ease)}
.faq[open] summary::after{content:"\2212"}
.faq summary:hover::after{color:var(--accent)}
.faq p{margin:0 0 18px;padding:0 2px;color:var(--mut);font-size:12.5px;line-height:1.7;max-width:66ch}

/* ---- site footer ---- */
#siteFoot{
  display:grid;gap:32px;grid-template-columns:1.4fr 1fr 1fr;
  max-width:1120px;width:100%;margin:0 auto;padding:34px 28px 26px;
}
#siteFoot b{display:block;font:500 10px/1 var(--mono);letter-spacing:.14em;text-transform:uppercase;color:var(--mut);margin-bottom:12px}
#siteFoot p{margin:0 0 8px;color:var(--mut);font-size:12px;line-height:1.65;max-width:32ch}
#siteFoot a{display:block;margin-bottom:7px;font:11px/1.5 var(--mono);color:var(--accent);text-decoration:none}
#siteFoot a:hover{text-decoration:underline;text-underline-offset:2px}
#siteFoot .by{color:var(--fg)}
#siteFoot .by a{display:inline}

/* ---- legal bar, the last line of the page ---- */
#footbar{
  display:flex;align-items:center;gap:12px;
  max-width:1120px;width:100%;margin:0 auto;padding:14px 28px 30px;
  border-top:1px solid var(--line);
  font:10px/1.6 var(--mono);letter-spacing:.06em;color:var(--dim);
}

.shake{animation:shake .34s}
@keyframes shake{
  10%,90%{transform:translateX(-2px)} 20%,80%{transform:translateX(4px)}
  30%,50%,70%{transform:translateX(-6px)} 40%,60%{transform:translateX(6px)}
}
.bad{border-color:var(--danger)!important;box-shadow:inset 0 0 0 1px var(--danger)!important}

/* ====================================================== modal & dialog */
#modal,#ask{position:fixed;inset:0;z-index:50;display:grid;place-items:center;padding:22px;background:rgba(0,0,0,.72);backdrop-filter:blur(3px)}
#ask{z-index:59}
[data-theme=light] #modal,[data-theme=light] #ask{background:rgba(255,255,255,.75)}
.sheet{
  width:100%;max-width:340px;background:var(--raise);
  border:1px solid var(--line2);padding:22px;
  max-height:92vh;overflow:auto;
  animation:rise .18s var(--ease);
}
@keyframes rise{from{opacity:0;transform:translateY(8px)}}
.lockmark{font-size:20px;margin-bottom:10px;line-height:1}
.sheet h2{margin:0;font:600 15px/1.3 var(--mono);letter-spacing:-.01em}
.msub{margin:8px 0 4px;color:var(--mut);font-size:12px;line-height:1.6}
.addpass{display:inline-block;margin-top:14px}
.mrow{display:flex;gap:8px;margin-top:20px}
.mrow .btn{height:36px}
#ask .in{margin-top:14px}

/* ====================================================== boot overlay */
#boot{position:fixed;inset:0;z-index:55;display:grid;place-items:center;background:var(--bg)}
.bootIn{text-align:center;max-width:280px;padding:20px}
.bootIn p{margin:14px 0 0;font:12px/1.5 var(--mono);color:var(--mut)}
#bootMsg{color:var(--fg)!important}
.spin{width:22px;height:22px;margin:0 auto;border:2px solid var(--line2);border-top-color:var(--accent);border-radius:50%;animation:spin .7s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}

/* =================================================== room-ended overlay */
#killed{position:fixed;inset:0;z-index:62;display:grid;place-items:center;padding:22px;background:rgba(0,0,0,.82);backdrop-filter:blur(3px)}
[data-theme=light] #killed{background:rgba(255,255,255,.85)}
.killedSheet{text-align:center}
.killedSheet .lockmark{font-size:26px;margin:0 0 12px}
.killedSheet .mrow{justify-content:center}
#killedTimer{margin-top:14px}

/* =============================================================== app */
#app{display:flex;flex-direction:column;height:100%}
#topbar{display:flex;align-items:center;gap:6px;height:var(--bar);padding:0 8px;border-bottom:1px solid var(--line);position:relative;flex:none}
.logo.sm{font-size:14px;padding:0 4px}
.chip{
  display:inline-flex;align-items:center;gap:6px;height:26px;padding:0 9px;
  border:1px solid var(--line2);background:var(--bg);
  font:600 12px/1 var(--mono);letter-spacing:.22em;
}
.chip:hover{border-color:var(--accent);color:var(--accent)}

/* avatar stack */
.stack{display:flex;align-items:center;padding-right:4px}
.av{
  width:24px;height:24px;border-radius:50%;flex:none;
  display:grid;place-items:center;position:relative;
  font:600 9px/1 var(--mono);color:#000;
  margin-left:-7px;box-shadow:0 0 0 2px var(--bg);
  cursor:pointer;transition:transform .16s var(--ease),filter .2s var(--ease);
}
.av:first-child{margin-left:0}
.av:hover{transform:translateY(-1px);z-index:3}
.av.me{cursor:default}
.av.idle{filter:grayscale(1) brightness(.62)}
.av.following{box-shadow:0 0 0 2px var(--bg),0 0 0 3px var(--accent)}
.av .live{position:absolute;right:-1px;bottom:-1px;width:8px;height:8px;border-radius:50%;background:var(--ok);box-shadow:0 0 0 2px var(--bg);animation:beat 1.1s infinite}
@keyframes beat{50%{transform:scale(.6);opacity:.55}}
.av.more{background:var(--panel);color:var(--mut);box-shadow:0 0 0 2px var(--bg),inset 0 0 0 1px var(--line2);cursor:pointer}
.av.more:hover{background:var(--line2);color:var(--fg)}

/* overflow / context menu */
#menu{
  position:fixed;top:calc(var(--bar) + 2px);right:8px;z-index:1000;min-width:220px;
  max-width:calc(100vw - 16px);max-height:calc(100vh - 60px);overflow-y:auto;
  -webkit-overflow-scrolling:touch;overscroll-behavior:contain;
  background:var(--raise);border:1px solid var(--line2);border-radius:var(--rad);padding:4px;
  box-shadow:0 14px 34px rgba(0,0,0,.7);
}
#menu button{
  display:flex;align-items:center;justify-content:space-between;gap:14px;width:100%;min-height:36px;
  text-align:left;padding:8px 12px;font:12px/1.2 var(--mono);color:var(--fg);border-radius:var(--rad);
  cursor:pointer;touch-action:manipulation;
}
#menu button:hover,#menu button:active{background:var(--panel);color:var(--accent)}
#menu kbd{font:10px/1 var(--mono);color:var(--dim);border:1px solid var(--line2);padding:2px 4px;border-radius:3px;flex-shrink:0}
.msep{height:1px;background:var(--line);margin:4px 0}

#banner{padding:7px 12px;background:var(--panel);border-bottom:1px solid var(--line);font:11px/1.5 var(--mono);color:var(--mut);flex:none}
#banner.warn{color:var(--warn);border-bottom-color:var(--warn)}
#banner.bad{color:var(--danger);border-bottom-color:var(--danger)}

/* tabs */
#tabbar{display:flex;align-items:center;gap:2px;height:30px;padding:0 6px;border-bottom:1px solid var(--line);overflow-x:auto;flex:none;scrollbar-width:none;-webkit-overflow-scrolling:touch;overscroll-behavior-x:contain}
#tabbar::-webkit-scrollbar{display:none}
#tabs{display:flex;gap:2px}
.tab{
  display:flex;align-items:center;gap:7px;height:22px;padding:0 9px;
  font:11px/1 var(--mono);color:var(--mut);white-space:nowrap;cursor:pointer;
  border:1px solid transparent;
  transition:background .14s var(--ease),color .14s var(--ease);
}
.tab:hover{color:var(--fg);background:var(--raise)}
.tab.on{color:var(--fg);background:var(--panel);border-color:var(--line2);box-shadow:inset 0 -2px 0 var(--accent)}
.tab .x{opacity:.45;font-size:13px}
.tab .x:hover{opacity:1;color:var(--danger)}

/* stage */
#stage{flex:1;display:flex;min-height:0;position:relative}
#editorWrap{flex:1;min-width:0;position:relative;overflow:hidden}
#editor{position:absolute;inset:0}
#overlay{position:absolute;inset:0;pointer-events:none;overflow:hidden;z-index:6}
.bubble{
  position:absolute;transform:translateY(-100%);white-space:nowrap;
  font:500 10px/1 var(--mono);color:#000;padding:3px 6px;
  animation:rise .14s var(--ease);
}
.bubble i{font-style:normal;letter-spacing:.1em;opacity:.75}

/* "3 edits below" pill */
#jump{
  position:absolute;left:50%;transform:translateX(-50%);bottom:14px;z-index:8;
  display:flex;align-items:center;gap:7px;padding:5px 11px;cursor:pointer;
  background:var(--fg);color:var(--bg);font:500 10px/1 var(--mono);
  letter-spacing:.1em;text-transform:uppercase;animation:rise .16s var(--ease);
}

/* codemirror */
.cm-editor{height:100%;background:transparent!important}
.cm-editor.cm-focused{outline:0!important}
.cm-scroller{font-family:var(--mono)!important;font-size:var(--edfont,13px);line-height:1.65;-webkit-overflow-scrolling:touch}
.cm-gutters{background:var(--bg)!important;border-right:1px solid var(--line)!important;color:var(--dim)!important}
.cm-activeLineGutter{background:var(--raise)!important;color:var(--mut)!important}
.cm-activeLine{background:rgba(255,255,255,.022)!important}
[data-theme=light] .cm-activeLine{background:rgba(0,0,0,.025)!important}
.cm-selectionBackground,.cm-content ::selection{background:#26436e!important}
[data-theme=light] .cm-selectionBackground{background:#cfe0fb!important}
.cm-cursor{border-left-color:var(--accent)!important;border-left-width:2px!important;animation:cmBlink 1.1s steps(1) infinite}
@keyframes cmBlink{50%{opacity:0}}
.cm-panels{background:var(--raise)!important;border-color:var(--line2)!important;font-family:var(--mono)}
.cm-searchMatch{background:rgba(232,179,57,.22)!important;outline:1px solid rgba(232,179,57,.5)}
.cm-searchMatch-selected{background:rgba(232,179,57,.45)!important}
.cm-tooltip{background:var(--raise)!important;border:1px solid var(--line2)!important;font-family:var(--mono)}
.cm-tooltip-autocomplete ul li[aria-selected]{background:var(--accent)!important;color:#fff!important}

/* presence gutter */
.cm-presence{min-width:20px;padding:0 2px}
.gmark{display:inline-grid;place-items:center;width:16px;height:16px;border-radius:50%;font:600 8px/1 var(--mono);color:#000}

/* remote carets, Docs style: name flag fades, returns on hover */
.cm-ySelection{border-radius:0}
.cm-ySelectionCaret{position:relative;border-left:2px solid;border-right:0;margin-left:-1px;margin-right:-1px;box-sizing:border-box}
.cm-ySelectionCaretDot{display:none}
.cm-ySelectionInfo{
  position:absolute;top:-15px;left:-2px;z-index:8;
  font:500 10px/14px var(--mono);color:#000;
  padding:0 5px;white-space:nowrap;pointer-events:none;
  opacity:1;transition:opacity .35s;
  animation:flagfade .35s 3s forwards;
}
@keyframes flagfade{to{opacity:0}}
.cm-ySelectionCaret:hover .cm-ySelectionInfo{opacity:1;animation:none}
/* touch screens have no hover, so give the name flag longer to be seen */
@media(hover:none){
  .cm-ySelectionInfo{animation:flagfade .35s 6s forwards}
}

/* side panels */
aside{position:relative;width:300px;flex:none;background:var(--panel);border-left:1px solid var(--line);padding:14px;overflow:auto;display:flex;flex-direction:column;z-index:20;-webkit-overflow-scrolling:touch;overscroll-behavior:contain}
.resizer{position:absolute;left:-3px;top:0;bottom:0;width:6px;cursor:col-resize;z-index:5}
.resizer:hover,.resizer.active{background:var(--accent)}
.phead{display:flex;align-items:center;justify-content:space-between;font:500 10px/1 var(--mono);letter-spacing:.14em;text-transform:uppercase;color:var(--mut);margin-bottom:12px}
.rowu{display:flex;align-items:center;gap:8px;padding:6px 0;font-size:12px;border-bottom:1px solid var(--line)}
.sw{width:8px;height:8px;border-radius:50%;flex:none}
.rowu .nm{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.rowu .tag{margin-left:auto;font:10px/1 var(--mono);color:var(--mut);text-transform:uppercase;letter-spacing:.1em}
.rowu .fbtn{font:10px/1 var(--mono);color:var(--accent);text-transform:uppercase;letter-spacing:.1em}
#panel .btn{width:100%;margin-top:8px}
#panel .mrow .btn{margin-top:0}
.plinks{display:flex;flex-direction:column;gap:8px;margin-top:12px;align-items:flex-start}

/* colour swatches */
#swatches{display:flex;gap:7px;flex-wrap:wrap}
#swatches .cs{width:20px;height:20px;border-radius:50%;cursor:pointer;box-shadow:0 0 0 2px var(--panel);transition:transform .14s var(--ease)}
#swatches .cs:hover{transform:scale(1.12)}
#swatches .cs.on{box-shadow:0 0 0 2px var(--panel),0 0 0 3px var(--fg)}

/* dark / light segmented control, in Settings */
.segrow{display:flex;border:1px solid var(--line2)}
.seg{
  flex:1;height:32px;background:var(--raise);color:var(--mut);
  font:500 11px/1 var(--mono);letter-spacing:.06em;text-transform:uppercase;
  transition:background .16s var(--ease),color .16s var(--ease);
}
.seg+.seg{border-left:1px solid var(--line2)}
.seg.on, .seg.active, .seg[aria-checked="true"]{background:var(--accent);color:#fff;box-shadow:0 0 8px rgba(76,141,255,.4)}

#chatList{flex:1;overflow:auto;display:flex;flex-direction:column;gap:11px;padding-bottom:10px;-webkit-overflow-scrolling:touch;overscroll-behavior:contain;scroll-behavior:smooth}
.msg .head{display:flex;align-items:center;gap:6px}
.msg .chip-av{width:16px;height:16px;border-radius:50%;flex:none;display:grid;place-items:center;font:600 8px/1 var(--mono);color:#000}
.msg .who{font:600 11px/1 var(--mono)}
.msg .when{color:var(--dim);font:10px/1 var(--mono);margin-left:auto;cursor:default}
.msg .body{white-space:pre-wrap;word-break:break-word;font-size:12.5px;margin-top:3px}
.msg .body .mention{color:var(--accent);font-weight:600}
.msg.hit{border-left:2px solid var(--accent);padding-left:8px}
.msg.sys{color:var(--mut);font:11px/1.5 var(--mono)}
.msg.grouped{margin-top:-5px}
#chatForm{flex:none;padding-top:8px;border-top:1px solid var(--line)}

/* status bar */
#statusbar{
  display:flex;align-items:center;gap:8px;height:24px;padding:0 10px;flex:none;
  border-top:1px solid var(--line);background:var(--raise);
  font:10px/1 var(--mono);letter-spacing:.08em;text-transform:uppercase;color:var(--mut);
}
#dot{width:6px;height:6px;border-radius:50%;background:var(--warn);flex:none}
#dot.on{background:var(--ok)}
#dot.off{background:var(--danger)}
.sep{width:1px;height:11px;background:var(--line2)}
.pill{padding:1px 6px;border:1px solid var(--line2);color:var(--mut)}
.pill.act{border-color:var(--accent);color:var(--accent)}
#lockBadge{border-color:var(--warn);color:var(--warn)}
#encBadge{color:var(--ok)}

/* ==================================================== command palette */
#pal{position:fixed;inset:0;z-index:58;display:grid;place-items:start center;padding:12vh 20px 20px;background:rgba(0,0,0,.6);backdrop-filter:blur(2px)}
[data-theme=light] #pal{background:rgba(255,255,255,.7)}
.palBox{width:100%;max-width:480px;background:var(--raise);border:1px solid var(--line2);box-shadow:0 24px 60px rgba(0,0,0,.6);animation:rise .14s var(--ease)}
.palIn{width:100%;height:44px;padding:0 14px;border:0;border-bottom:1px solid var(--line);background:transparent;color:var(--fg);font:13px/1 var(--mono)}
.palIn:focus{outline:0}
#palList{max-height:46vh;overflow:auto;padding:4px;-webkit-overflow-scrolling:touch;overscroll-behavior:contain}
#palList .pi{display:flex;align-items:center;gap:10px;padding:8px 10px;font:12px/1 var(--mono);color:var(--fg);cursor:pointer;transition:background .12s var(--ease)}
#palList .pi .kb{margin-left:auto;color:var(--dim);font-size:10px}
#palList .pi.on{background:var(--accent);color:#fff}
#palList .pi.on .kb{color:rgba(255,255,255,.75)}
#palList .none{padding:14px 10px;color:var(--mut);font:12px/1 var(--mono)}

/* mobile bar */
#mbar{display:none;height:calc(46px + env(safe-area-inset-bottom));padding-bottom:env(safe-area-inset-bottom);border-top:1px solid var(--line);background:var(--raise)}
#mbar button{flex:1;font:10px/1 var(--mono);letter-spacing:.12em;text-transform:uppercase;color:var(--mut)}
#mbar button:active{color:var(--accent)}

#toast{
  position:fixed;left:50%;bottom:calc(38px + env(safe-area-inset-bottom));transform:translate(-50%,8px);z-index:60;
  background:var(--fg);color:var(--bg);padding:7px 12px;max-width:82vw;text-align:center;
  font:500 11px/1.4 var(--mono);opacity:0;transition:.2s var(--ease);pointer-events:none;
}
#toast.show{opacity:1;transform:translate(-50%,0)}

/* ================================================== standalone docs */
/* privacy.html, terms.html and security.html share the app shell but
   scroll like ordinary documents. */
body.doc-page{overflow:auto;scroll-behavior:smooth;-webkit-overflow-scrolling:touch;overscroll-behavior-y:contain}
.doc{max-width:660px;margin:0 auto;padding:56px 24px 80px}
.doc h1{font:600 24px/1.25 var(--sans);letter-spacing:-.02em;margin:20px 0 6px}
.doc .updated{color:var(--dim);font:10px/1 var(--mono);letter-spacing:.1em;text-transform:uppercase;margin:0 0 8px}
.doc h2{font:600 13px/1 var(--mono);letter-spacing:.02em;margin:40px 0 12px;padding-top:18px;border-top:1px solid var(--line)}
.doc p,.doc li{color:var(--mut);font-size:13px;line-height:1.75}
.doc strong{color:var(--fg);font-weight:600}
.doc code{font-family:var(--mono);font-size:12px;color:var(--fg);background:var(--panel);padding:1px 5px}
.doc pre{background:var(--panel);border:1px solid var(--line);padding:14px;overflow:auto;font:12px/1.6 var(--mono);color:var(--fg)}
.doc ul{padding-left:18px}
.doc a{color:var(--accent)}
.doc .back{display:inline-block;font:11px/1 var(--mono);color:var(--mut);text-decoration:none}
.doc .back:hover{color:var(--accent)}
.doc .warn{border-left:2px solid var(--warn);padding-left:14px}
.doc .warn strong{color:var(--warn)}
.doc .foot{margin-top:52px;padding-top:16px;border-top:1px solid var(--line);color:var(--dim);font:10px/1.7 var(--mono);letter-spacing:.06em}

/* ======================================================= responsive */
@media(min-width:1400px){
  body{font-size:14px}
  .btn{height:30px;padding:0 12px;font-size:12.5px}
  .btn.ico{width:30px}
  .in{height:38px}
  :root{--bar:42px;--edfont:14px}
}
@media(max-width:1080px){
  aside{position:absolute;top:0;right:0;bottom:0;width:300px!important}
  .resizer{display:none}
  .hero{grid-template-columns:1fr;gap:36px;padding:44px 24px 32px}
  .gwrap{max-width:none}
  .htitle{max-width:20ch}
  .demoWrap{height:290px}
  .strips{grid-template-columns:1fr}
  #siteFoot{grid-template-columns:1fr 1fr}
  .steps{grid-template-columns:1fr}
  .howBlock,.faqBlock{padding:40px 24px}
}
@media(max-width:720px){
  aside{left:0;width:auto!important;top:auto;height:64%;border-left:0;border-top:1px solid var(--line2);transform:translateY(0);transition:transform .28s var(--ease),opacity .22s var(--ease);will-change:transform}
  aside.closing{transform:translateY(100%);opacity:.5;pointer-events:none}
  #mbar{display:flex}
  .hideS{display:none!important}
  #topbar{gap:4px;overflow-x:auto;-webkit-overflow-scrolling:touch;scrollbar-width:none}
  #topbar::-webkit-scrollbar{display:none}
  #editorContainer{flex-direction:column}
  #previewWrap{height:50%;border-left:0;border-top:1px solid var(--line)}
  .wb-toolbar{padding:4px 8px;gap:6px;overflow-x:auto;-webkit-overflow-scrolling:touch;scrollbar-width:none}
  .wb-toolbar::-webkit-scrollbar{display:none}
  #terminal{height:50%;max-height:50vh}
  #historyDrawer{height:50%;max-height:50vh}
  .term-head,.phead{min-height:36px}
  .cm-gutters{display:none}
  #statusbar{overflow:hidden}
  .hero{padding:32px 18px 24px}
  .htitle{font-size:23px}
  .demoWrap{height:230px}
  .strip{padding:20px}
  #siteFoot{grid-template-columns:1fr;gap:26px;padding:26px 18px 20px}
  #footbar{padding:14px 18px 30px}
  #pal{padding:8vh 14px 14px}
  .cm-cursor{border-left-width:3px!important}
  .cm-ySelectionCaret{border-left-width:3px}
  .cm-activeLine{background:rgba(76,141,255,.14)!important}
  .sectitle{font-size:17px}
  .howBlock,.faqBlock{padding:32px 18px}
  .step{padding:20px}
  .faq summary{padding:14px 2px;font-size:12.5px}
  .strip:hover,.step:hover{transform:none}
}

/* -------------------------------------------------- stage & split preview */
#editorContainer{display:flex;flex:1;min-height:0;min-width:0;position:relative}
#editorWrap{flex:1;min-width:0;position:relative;height:100%}
#previewWrap{
  flex:1;min-width:0;height:100%;border-left:1px solid var(--line);
  display:flex;flex-direction:column;background:var(--bg);overflow:hidden;
}
.preview-head{
  border-bottom:1px solid var(--line);padding:6px 10px;display:flex;align-items:center;
  gap:8px;background:var(--raise);flex:none;
}
.preview-title{font:600 11px/1 var(--mono);color:var(--fg);white-space:nowrap}
.segrow.sm{height:24px;border:1px solid var(--line2);display:inline-flex}
.segrow.sm .seg{padding:0 8px;font:500 10px/22px var(--mono);background:var(--panel);border:0;color:var(--mut);cursor:pointer}
.segrow.sm .seg.active{background:var(--accent);color:#fff}
.preview-pane{flex:1;overflow-y:auto;padding:16px;font-family:var(--sans);color:var(--fg);border:0}
.preview-pane iframe{width:100%;height:100%;border:0}
.pdf-preview-pane{padding:0!important;background:#2a2a2e;width:100%;height:100%;border:0}
.doc-preview-pane{
  flex:1;overflow-y:auto;padding:24px 28px;background:var(--bg);color:var(--fg);
  font:400 13.5px/1.7 var(--sans);
}
.docx-document{max-width:760px;margin:0 auto}
.docx-h1{font:700 20px/1.3 var(--sans);margin:18px 0 10px;color:var(--fg);border-bottom:1px solid var(--line);padding-bottom:6px}
.docx-h2{font:600 16px/1.3 var(--sans);margin:14px 0 8px;color:var(--fg)}
.docx-h3{font:600 14px/1.3 var(--sans);margin:10px 0 6px;color:var(--fg)}
.docx-p{margin:8px 0;line-height:1.7}
.docx-table{width:100%;border-collapse:collapse;margin:14px 0;font-size:12.5px}
.docx-table td, .docx-table th{border:1px solid var(--line2);padding:7px 10px}
.docx-table tr:nth-child(even){background:var(--raise)}
.docx-empty{color:var(--mut);font-style:italic;padding:20px;text-align:center}
.docx-error{color:var(--danger);background:rgba(255,77,79,.1);border:1px solid var(--danger);padding:12px;border-radius:4px}
.md-rendered{font-size:13px;line-height:1.6}
.md-rendered h1,.md-rendered h2,.md-rendered h3{margin:16px 0 8px;font-family:var(--mono)}
.md-rendered code{background:var(--panel);padding:2px 5px;font-family:var(--mono);font-size:12px;border-radius:3px}
.md-rendered pre{background:var(--panel);padding:12px;border:1px solid var(--line);border-radius:4px;overflow-x:auto}
.md-rendered pre code{background:transparent;padding:0}

/* ---------------------------------------------------- console / runner */
#terminal{
  position:absolute;left:0;right:0;bottom:0;height:260px;z-index:20;
  background:#09090b;color:#f4f4f5;border-top:1px solid var(--line);
  display:flex;flex-direction:column;box-shadow:0 -4px 16px rgba(0,0,0,.4);
}
.term-head{
  height:34px;display:flex;align-items:center;gap:8px;padding:0 10px;
  background:#121215;border-bottom:1px solid #202025;font:11px/1 var(--mono);flex:none;
}
.term-title{font-weight:600;letter-spacing:.05em;color:#a1a1aa}
.term-content-pane{flex:1;min-height:0;display:flex;flex-direction:column;position:relative}
.term-body{
  flex:1;margin:0;padding:10px 12px;font:12px/1.5 var(--mono);
  overflow-y:auto;white-space:pre-wrap;word-break:break-all;color:#e4e4e7;
}
.term-body:empty::before{content:"Program output will appear here...";color:#52525b}
.term-prompt-banner{
  padding:6px 10px;background:rgba(76,141,255,.15);border-bottom:1px solid rgba(76,141,255,.3);
  display:flex;align-items:center;justify-content:space-between;gap:8px;font:11px/1.3 var(--mono);color:#93c5fd;
}
.term-stdin-header{
  padding:6px 10px;display:flex;align-items:center;justify-content:space-between;
  background:#18181b;border-bottom:1px solid #27272a;font:11px/1 var(--mono);color:#a1a1aa;
}
.term-stdin-area{
  flex:1;width:100%;padding:10px 12px;border:none;outline:none;resize:none;
  background:#09090b;color:#38bdf8;font:12px/1.5 var(--mono);
}
.term-foot{
  height:36px;display:flex;align-items:center;gap:8px;padding:0 10px;
  background:#121215;border-top:1px solid #202025;flex:none;
}
.term-stdin-hint{font:10px/1 var(--mono);color:#71717a}

/* --------------------------------------------------- Media & File Sharing Hub */
#fileDrawer{
  position:absolute;top:0;right:0;bottom:0;width:420px;max-width:100vw;z-index:25;
  background:var(--panel);border-left:1px solid var(--line);
  display:flex;flex-direction:column;box-shadow:-4px 0 20px rgba(0,0,0,.35);
}
.file-hub-head{
  display:flex;align-items:center;justify-content:space-between;padding:10px 14px;
  background:var(--raise);border-bottom:1px solid var(--line);
}
.file-hub-title-wrap{display:flex;flex-direction:column;gap:2px}
.file-hub-title{font:600 13px/1 var(--mono);color:var(--fg)}
.file-hub-subtitle{font:10px/1 var(--sans);color:var(--mut)}
.file-body{flex:1;overflow-y:auto;padding:14px;display:flex;flex-direction:column;gap:12px}
.dropzone{
  border:2px dashed var(--line2);padding:22px 14px;text-align:center;
  background:var(--raise);cursor:pointer;transition:border-color .18s,background .18s;border-radius:4px;
}
.dropzone:hover, .dropzone.drop-active{border-color:var(--accent);background:rgba(96,165,250,.08);box-shadow:0 0 0 2px rgba(96,165,250,.2)}
.drop-lead{margin:8px 0 4px;font:500 12px/1.4 var(--sans);color:var(--fg)}
.drop-sub{margin:4px 0 0;font-size:11px;color:var(--mut)}
.file-browse-btn{cursor:pointer;display:inline-block;margin-left:4px}
.progress-bar-wrap{margin-top:12px;background:var(--bg);height:16px;position:relative;border:1px solid var(--line);border-radius:3px;overflow:hidden}
.progress-bar{height:100%;background:var(--accent);transition:width .12s}
.progress-bar-wrap span{position:absolute;top:0;left:0;right:0;bottom:0;font:600 10px/16px var(--mono);text-align:center;color:#fff}

.file-filter-bar{display:flex;align-items:center;font:11px/1 var(--mono);color:var(--mut);padding:2px 0}
.shared-list{display:flex;flex-direction:column;gap:10px}
.shared-card{
  background:var(--raise);border:1px solid var(--line);border-radius:4px;
  display:flex;flex-direction:column;overflow:hidden;transition:border-color .15s;
}
.shared-card:hover{border-color:var(--accent)}
.shared-card-head{display:flex;align-items:center;gap:8px;padding:8px 10px;background:var(--panel)}
.file-type-badge{
  font:600 9px/1 var(--mono);padding:2px 5px;border-radius:2px;
  background:var(--line2);color:var(--fg);text-transform:uppercase;
}
.file-type-badge.video{background:#818cf8;color:#000}
.file-type-badge.image{background:#38bdf8;color:#000}
.file-type-badge.pdf{background:#f87171;color:#000}
.file-type-badge.docx, .file-type-badge.doc{background:#60a5fa;color:#000}
.file-type-badge.audio{background:#34d399;color:#000}
.shared-card-name{font:600 11px/1.2 var(--mono);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1}
.shared-card-size{font:10px/1 var(--mono);color:var(--mut)}
.shared-media-preview{
  width:100%;max-height:220px;background:#000;display:flex;align-items:center;justify-content:center;
  overflow:hidden;border-top:1px solid var(--line);border-bottom:1px solid var(--line);
}
.shared-video-player{width:100%;max-height:220px;outline:none}
.shared-img-preview{max-width:100%;max-height:220px;object-fit:contain}
.shared-card-foot{display:flex;align-items:center;gap:6px;padding:6px 10px;background:var(--raise)}
.shared-card-foot .grow{flex:1}

/* Standardized, prominent close button for panels, drawers & modals */
.pclose-btn, .panel-close-btn{
  width: 28px;
  height: 28px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--raise);
  border: 1px solid var(--line2);
  border-radius: 4px;
  color: var(--mut);
  cursor: pointer;
  padding: 0;
  flex: none;
  line-height: 1;
  transition: all .15s var(--ease);
}
.pclose-btn:hover, .panel-close-btn:hover{
  background: var(--line);
  color: var(--fg);
  border-color: var(--mut);
  transform: scale(1.05);
}
.pclose-btn:active, .panel-close-btn:active{
  transform: scale(0.92);
  background: var(--accent);
  color: #fff;
  border-color: var(--accent);
}
.pclose-btn svg, .panel-close-btn svg{
  width: 14px;
  height: 14px;
  stroke: currentColor;
  stroke-width: 2.2;
  fill: none;
  pointer-events: none;
}
.phead-actions{display:flex;align-items:center;gap:6px;margin-left:auto}

/* --------------------------------------------------- revision history / time machine */
#historyDrawer{
  position:absolute;left:0;right:0;bottom:0;height:290px;z-index:22;
  background:var(--raise);border-top:1px solid var(--line);
  display:flex;flex-direction:column;box-shadow:0 -6px 24px rgba(0,0,0,.5);
}
.hist-phead{
  display:flex;align-items:center;gap:12px;padding:8px 14px;
  border-bottom:1px solid var(--line);background:var(--panel);min-height:40px;margin-bottom:0;
}
.hist-title-wrap{display:flex;align-items:center;gap:8px}
.hist-title{font:700 12px/1 var(--mono);color:var(--fg);letter-spacing:.04em}
.hist-view-switch{display:flex;border:1px solid var(--line2);border-radius:3px;overflow:hidden}
.hist-view-switch .seg{padding:0 8px;height:24px;font-size:10px}
.hist-body{flex:1;display:flex;flex-direction:column;overflow:hidden}
.hist-controls{display:flex;align-items:center;gap:8px;padding:8px 14px;background:var(--raise);border-bottom:1px solid var(--line)}
.hist-slider-wrap{flex:1;display:flex;flex-direction:column;gap:4px}
.hist-slider-wrap .slider{
  width:100%;-webkit-appearance:none;appearance:none;height:6px;
  background:var(--line2);outline:0;border-radius:3px;cursor:pointer;
}
.hist-slider-wrap .slider::-webkit-slider-thumb{
  -webkit-appearance:none;width:16px;height:16px;border-radius:50%;
  background:var(--accent);cursor:grab;box-shadow:0 0 6px rgba(76,141,255,.6);
  border:2px solid #fff;
}
.hist-slider-wrap .slider::-webkit-slider-thumb:active{cursor:grabbing;transform:scale(1.15)}
.hist-slider-meta{display:flex;justify-content:space-between;align-items:center;font:11px/1 var(--mono);color:var(--mut)}
.hist-view{flex:1;margin:0;padding:12px;overflow-y:auto;font:12px/1.5 var(--mono);background:var(--bg)}
.hist-snap-view{white-space:pre-wrap;word-break:break-all;color:var(--fg)}

/* Inactivity countdown warning modal */
#inactivity{
  position:fixed;inset:0;background:rgba(0,0,0,.75);
  display:grid;place-items:center;padding:16px;z-index:99;
  backdrop-filter:blur(4px);
}
.inactivitySheet{
  border:1px solid var(--warn, #ffb347)!important;
  max-width:440px;width:100%;
  box-shadow:0 16px 48px rgba(0,0,0,.7);
  animation:pop-in .2s ease-out;
}
.inactivity-countdown{
  font:700 38px/1.2 var(--mono);color:var(--warn, #ffb347);text-align:center;
  margin:16px 0;letter-spacing:.08em;padding:14px;background:var(--raise);
  border:1px solid var(--line2);border-radius:4px;
  box-shadow:inset 0 0 12px rgba(255,179,71,.15);
}

/* --------------------------------------------------- mobile accessory keys */
.mobile-keys-bar{
  display:none;overflow-x:auto;-webkit-overflow-scrolling:touch;
  height:32px;background:var(--raise);border-top:1px solid var(--line);
  padding:0 4px;gap:4px;align-items:center;
}
.mobile-keys-bar button{
  height:24px;min-width:28px;padding:0 6px;flex:none;
  background:var(--panel);border:1px solid var(--line2);
  font:600 12px/1 var(--mono);color:var(--fg);border-radius:2px;
}
.mobile-keys-bar button:active{background:var(--accent);color:#fff}
@media(max-width:720px){
  .mobile-keys-bar{display:flex}
}

/* --------------------------------------------------- recent rooms drawer */
#bookmarksDrawer{
  position:absolute;top:0;right:0;bottom:0;width:320px;z-index:25;
  background:var(--panel);border-left:1px solid var(--line);
  display:flex;flex-direction:column;box-shadow:-4px 0 16px rgba(0,0,0,.3);
}
.bookmarks-body{flex:1;overflow-y:auto;padding:12px;display:flex;flex-direction:column;gap:8px}
.bookmark-card{
  display:flex;flex-direction:column;gap:4px;padding:10px;
  background:var(--raise);border:1px solid var(--line);border-radius:4px;
  cursor:pointer;transition:border-color .15s, background .15s;
}
.bookmark-card:hover{border-color:var(--accent);background:var(--bg)}
.bookmark-head{display:flex;align-items:center;justify-content:space-between;gap:6px}
.bookmark-code{font:700 12px/1 var(--mono);color:var(--accent)}
.bookmark-role{font:9px/1 var(--mono);padding:2px 5px;border-radius:2px;background:var(--line);color:var(--fg)}
.bookmark-role.owner{background:rgba(255,165,0,.2);color:#ffa500}
.bookmark-meta{font-size:10px;color:var(--mut);display:flex;justify-content:space-between}
.bookmark-del{color:var(--mut);background:none;border:none;cursor:pointer;padding:2px 4px;font-size:12px}
.bookmark-del:hover{color:#ff4d4f}

/* --------------------------------------------------- zen mode */
body.zen-mode #topbar,
body.zen-mode #tabbar,
body.zen-mode #statusbar,
body.zen-mode #mobileKeys,
body.zen-mode #mbar{
  display:none !important;
}
body.zen-mode #stage{
  height:100vh !important;
}
.floating-zen-btn{
  position:fixed;bottom:18px;right:18px;z-index:100;
  backdrop-filter:blur(8px);box-shadow:0 4px 16px rgba(0,0,0,.5);
  opacity:.7;transition:opacity .2s;
}
.floating-zen-btn:hover{opacity:1}

/* --------------------------------------------------- visual diff viewer */
.diff-container{display:flex;flex-direction:column;font:12px/1.5 var(--mono);background:var(--bg)}
.diff-line{display:flex;align-items:flex-start;padding:1px 6px;white-space:pre-wrap;word-break:break-all}
.diff-marker{width:16px;flex:none;font-weight:bold;user-select:none}
.diff-content{flex:1}
.diff-add{background:rgba(46,160,67,.18);color:#56d364}
.diff-add .diff-marker{color:#3fb950}
.diff-del{background:rgba(248,81,73,.18);color:#ff7b72;text-decoration:line-through}
.diff-del .diff-marker{color:#f85149}
.diff-same{color:var(--fg);opacity:.75}

/* --------------------------------------------------- compiler error highlights */
.cm-error-line{background:rgba(255,77,79,.12) !important;border-left:3px solid #ff4d4f !important}
.term-err-link{
  color:#4c8dff;text-decoration:underline;cursor:pointer;font-weight:600;
}
.term-err-link:hover{color:#79a9ff}

/* --------------------------------------------------- drag & drop active */
#editorWrap.editor-drop-active{
  outline:2px dashed var(--accent);outline-offset:-4px;background:rgba(76,141,255,.05);
}

/* --------------------------------------------------- collaborative room goal banner */
.room-goal-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 6px 14px;
  background: linear-gradient(90deg, rgba(76, 141, 255, 0.12) 0%, rgba(199, 146, 234, 0.12) 100%);
  border-bottom: 1px solid var(--line2);
  gap: 10px;
  flex: none;
  animation: fadein 0.2s var(--ease);
}
.room-goal-banner.goal-done {
  background: linear-gradient(90deg, rgba(61, 220, 132, 0.15) 0%, rgba(79, 214, 210, 0.15) 100%);
  border-color: rgba(61, 220, 132, 0.35);
}
.goal-left { display: flex; align-items: center; gap: 8px; min-width: 0; flex: 1; }
.goal-pill { font: 700 9px/1.2 var(--mono); padding: 3px 6px; border-radius: 3px; background: var(--accent); color: #fff; letter-spacing: 0.08em; flex: none; }
.goal-text { font: 500 12.5px/1.3 var(--sans); color: var(--fg); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.goal-done .goal-text { text-decoration: line-through; opacity: 0.8; }
.goal-status-pill { font-size: 10px; flex: none; background: rgba(255, 179, 71, 0.2); color: #ffb347; border: 1px solid rgba(255, 179, 71, 0.4); }
.goal-done .goal-status-pill { background: rgba(61, 220, 132, 0.2); color: #3ddc84; border-color: rgba(61, 220, 132, 0.4); }
.goal-right { display: flex; align-items: center; gap: 6px; flex: none; }
.goal-act-btn { font-size: 11px; padding: 0 8px; height: 24px; }
.goal-close-btn { width: 24px; height: 24px; border: none; background: none; color: var(--mut); cursor: pointer; display: flex; align-items: center; justify-content: center; }
.goal-close-btn:hover { color: var(--danger); }
.goal-close-btn svg { width: 14px; height: 14px; stroke: currentColor; stroke-width: 2; fill: none; }

/* --------------------------------------------------- in-browser web & docs viewer */
#browserDrawer {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  width: 480px;
  z-index: 24;
  background: var(--panel);
  border-left: 1px solid var(--line);
  display: flex;
  flex-direction: column;
  box-shadow: -4px 0 20px rgba(0,0,0,.45);
}
.browser-phead { margin-bottom: 0; padding: 8px 12px; border-bottom: 1px solid var(--line); }
.browser-head-title { display: flex; align-items: center; gap: 8px; font-weight: 700; color: var(--fg); }
.browser-toolbar {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 10px;
  background: var(--raise);
  border-bottom: 1px solid var(--line2);
}
.browser-url-in {
  flex: 1;
  height: 28px;
  font: 11.5px/1 var(--mono);
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: 4px;
  padding: 0 8px;
  color: var(--fg);
}
.browser-chips {
  display: flex;
  gap: 6px;
  padding: 6px 10px;
  background: var(--panel);
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
  border-bottom: 1px solid var(--line);
}
.browser-chips::-webkit-scrollbar { display: none; }
.b-chip {
  padding: 3px 8px;
  border-radius: 12px;
  font: 500 10.5px/1 var(--mono);
  background: var(--raise);
  border: 1px solid var(--line2);
  color: var(--mut);
  cursor: pointer;
  white-space: nowrap;
  transition: color 0.15s, border-color 0.15s;
}
.b-chip:hover { color: var(--accent); border-color: var(--accent); }
.browser-body { flex: 1; position: relative; background: #fff; display: flex; flex-direction: column; }
.browser-iframe { width: 100%; height: 100%; border: none; }
.browser-frame-notice {
  position: absolute;
  bottom: 16px;
  left: 16px;
  right: 16px;
  padding: 12px 14px;
  background: rgba(15, 23, 42, 0.94);
  border: 1px solid #334155;
  border-radius: 8px;
  color: #f8fafc;
  font: 12px/1.5 var(--sans);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.5);
}

/* --------------------------------------------------- teamwork multi-device preview */
.device-seg { margin-left: 6px; }
.preview-frame-holder {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg);
  overflow: hidden;
  position: relative;
  width: 100%;
}
.preview-frame-holder.device-tablet #htmlPreview {
  max-width: 768px;
  height: 94%;
  border: 1px solid var(--line2);
  border-radius: 12px;
  box-shadow: 0 8px 30px rgba(0,0,0,0.4);
}
.preview-frame-holder.device-mobile #htmlPreview {
  max-width: 375px;
  height: 92%;
  border: 1px solid var(--line2);
  border-radius: 24px;
  box-shadow: 0 8px 32px rgba(0,0,0,0.5);
}
.preview-frame-holder.device-landscape #htmlPreview {
  max-width: 667px !important;
  height: 375px !important;
}
.teamwork-bar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 4px 10px;
  background: var(--raise);
  border-bottom: 1px solid var(--line);
  font: 500 11px/1 var(--mono);
  color: var(--mut);
}
.tw-badge { color: var(--accent); font-weight: 600; display: inline-flex; align-items: center; gap: 4px; }
.tw-console-pane {
  height: 110px;
  border-top: 1px solid var(--line);
  background: #09090b;
  display: flex;
  flex-direction: column;
}
.tw-console-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 4px 10px;
  background: #141417;
  font: 600 10.5px/1 var(--mono);
  color: #a1a1aa;
  border-bottom: 1px solid #202024;
}
.tw-console-logs {
  flex: 1;
  margin: 0;
  padding: 6px 10px;
  font: 11.5px/1.4 var(--mono);
  color: #e4e4e7;
  overflow-y: auto;
  white-space: pre-wrap;
}
.tw-c-log { color: #e4e4e7; line-height: 1.5; border-bottom: 1px solid rgba(255,255,255,0.04); padding: 2px 0; }
.tw-c-warn { color: #f59e0b; line-height: 1.5; border-bottom: 1px solid rgba(245,158,11,0.1); padding: 2px 0; }
.tw-c-error { color: #ff5c4d; line-height: 1.5; border-bottom: 1px solid rgba(255,92,77,0.15); padding: 2px 0; }

/* --------------------------------------------------- turbo boost developer mode */
.turbo-pill {
  background: linear-gradient(135deg, #f59e0b, #ef4444) !important;
  color: #fff !important;
  font-weight: 700 !important;
  box-shadow: 0 0 10px rgba(245, 158, 11, 0.5);
  cursor: pointer;
  border: 0 !important;
}
body.turbo-boost #stage {
  box-shadow: inset 0 0 0 1px rgba(245, 158, 11, 0.35);
}

/* --------------------------------------------------- generative UI modal */
#genUiModal {
  position: fixed; inset: 0; z-index: 70;
  background: rgba(0,0,0,.65); backdrop-filter: blur(8px);
  display: flex; align-items: center; justify-content: center;
  padding: 16px;
}
.gen-ui-sheet {
  width: 100%; max-width: 680px; max-height: 90vh;
  background: var(--panel); border: 1px solid var(--line2);
  border-radius: 14px; padding: 20px;
  display: flex; flex-direction: column; overflow-y: auto;
  box-shadow: 0 20px 48px rgba(0,0,0,0.6);
}
.gen-phead { margin-bottom: 8px; }
.gen-ui-head-title { display: flex; align-items: center; gap: 8px; }
.gen-ui-head-title h2 { font-size: 17px; font-weight: 700; color: var(--fg); margin: 0; }
.gen-input-wrap { display: flex; gap: 8px; margin: 14px 0; }
.gen-preset-grid {
  display: grid; grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
  gap: 8px; margin: 10px 0 16px;
}
.gen-preset-chip {
  padding: 8px 10px; border-radius: 8px;
  background: var(--raise); border: 1px solid var(--line);
  color: var(--fg); font: 500 12px/1.3 var(--sans);
  cursor: pointer; display: flex; align-items: center; gap: 6px;
  transition: border-color 0.15s, background 0.15s;
}
.gen-preset-chip:hover { border-color: var(--accent); background: rgba(76, 141, 255, 0.1); }
.gen-preview-box {
  height: 220px; border: 1px solid var(--line);
  border-radius: 8px; overflow: hidden; background: #000; margin-bottom: 16px;
}
.gen-preview-frame { width: 100%; height: 100%; border: none; }
.gen-actions-row { display: flex; gap: 10px; justify-content: flex-end; }

/* --------------------------------------------------- chat slash commands popover */
.chat-slash-popup {
  position: absolute;
  bottom: 46px;
  left: 8px;
  right: 8px;
  background: var(--panel);
  border: 1px solid var(--line2);
  border-radius: 8px;
  padding: 4px;
  box-shadow: 0 -8px 24px rgba(0,0,0,0.45);
  max-height: 220px;
  overflow-y: auto;
  z-index: 30;
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.slash-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 6px 10px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
  color: var(--fg);
  transition: background 0.12s;
}
.slash-item:hover, .slash-item.selected {
  background: var(--accent);
  color: #fff;
}
.slash-cmd { font: 600 12px/1 var(--mono); }
.slash-desc { font-size: 11px; opacity: 0.8; margin-left: 8px; }

/* --------------------------------------------------- mobile polish & active states */
#mbar button.active, #mbar button[aria-pressed="true"] {
  color: #fff !important;
  font-weight: 700;
  background: var(--accent) !important;
  box-shadow: 0 0 10px rgba(76, 141, 255, 0.45);
  border-radius: 4px;
}
@media (max-width: 720px) {
  #browserDrawer, #fileDrawer, #bookmarksDrawer {
    left: 0 !important;
    right: 0 !important;
    width: auto !important;
    top: auto !important;
    height: 68% !important;
    max-width: 100vw !important;
    border-left: 0 !important;
    border-top: 1px solid var(--line2) !important;
    transform: translateY(0);
    transition: transform .28s var(--ease), opacity .22s var(--ease);
    will-change: transform;
  }
  #browserDrawer.closing, #fileDrawer.closing, #bookmarksDrawer.closing {
    transform: translateY(100%) !important;
    opacity: .5 !important;
    pointer-events: none !important;
  }
  .pclose-btn { min-width: 44px; min-height: 44px; }
  #mbar button { min-height: 44px; padding: 12px 4px; }
}

@media (max-width: 600px) {
  .gen-actions-row {
    flex-direction: column;
    gap: 8px;
  }
  .gen-actions-row button {
    width: 100%;
    min-height: 44px;
  }
  .gen-input-wrap {
    flex-direction: column;
    gap: 8px;
  }
  .gen-input-wrap button {
    width: 100%;
    min-height: 44px;
  }
}

/* --------------------------------------------------- Deafen / Speaker Mute */
#voiceDeafenBtn.deafened, #voiceDeafenBtn[aria-pressed="true"] {
  background: rgba(239, 68, 68, 0.25) !important;
  border-color: #ef4444 !important;
  color: #ef4444 !important;
  box-shadow: 0 0 10px rgba(239, 68, 68, 0.45) !important;
}
#voiceDeafenBtn.deafened svg { stroke: #ef4444 !important; }

/* --------------------------------------------------- KaTeX Math in Preview */
.math-block, .katex-display {
  display: block;
  margin: 14px 0;
  padding: 10px 14px;
  overflow-x: auto;
  overflow-y: hidden;
  text-align: center;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid var(--line);
  border-radius: 4px;
}
.math-inline, .katex {
  display: inline-block;
  vertical-align: middle;
  padding: 0 2px;
}

/* --------------------------------------------------- Collaborative Whiteboard */
.whiteboard-wrap {
  flex: 1;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  position: relative;
  height: 100%;
  min-height: 0;
  overflow: hidden;
}
.wb-toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 12px;
  background: var(--panel);
  border-bottom: 1px solid var(--line);
  flex-wrap: wrap;
  z-index: 10;
  flex-shrink: 0;
}
.wb-tool-group, .wb-color-group, .wb-size-group, .wb-action-group {
  display: flex;
  align-items: center;
  gap: 6px;
}
.wb-tool-btn.active {
  background: var(--accent) !important;
  border-color: var(--accent) !important;
  color: #fff !important;
}
.wb-color-dot {
  width: 20px;
  height: 20px;
  border-radius: 50%;
  border: 2px solid transparent;
  cursor: pointer;
  transition: transform .15s var(--ease), border-color .15s var(--ease);
}
.wb-color-dot:hover { transform: scale(1.15); }
.wb-color-dot.active {
  border-color: #fff;
  transform: scale(1.25);
  box-shadow: 0 0 6px rgba(255,255,255,0.7);
}
.wb-canvas-holder {
  flex: 1;
  position: relative;
  width: 100%;
  height: 100%;
  min-height: 0;
  touch-action: none;
  overflow: hidden;
  background: #080808;
}
[data-theme=light] .wb-canvas-holder { background: #fdfdfd; }
#whiteboardCanvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  cursor: crosshair;
  touch-action: none;
}

/* --------------------------------------------------- CodeMirror Gutter Error Markers */
.cm-error-gutter-marker {
  color: var(--danger);
  font-weight: 800;
  font-size: 13px;
  text-align: center;
  cursor: pointer;
  line-height: 1;
  display: inline-block;
  padding: 0 2px;
}
.cm-error-line-highlight {
  background: rgba(255, 92, 77, 0.12) !important;
}

/* --------------------------------------------------- Status Bar Live Counts & Follow Badge */
#counts {
  font: 11px/1.2 var(--mono);
  color: var(--mut);
  white-space: nowrap;
  letter-spacing: 0.02em;
}
#followBadge.act {
  background: var(--accent);
  color: #fff;
  border-color: var(--accent);
  cursor: pointer;
}
#followBadge:hover {
  filter: brightness(1.15);
}